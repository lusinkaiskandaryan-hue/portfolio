# Portfolio Starter

Starter codebase for a frontend portfolio with React, TypeScript, Vite, Ant Design, and pnpm.

## Getting started

```bash
pnpm install
pnpm dev
```

## Scripts

- `pnpm dev` - start local dev server
- `pnpm build` - type-check and build production bundle
- `pnpm preview` - preview production build
- `pnpm lint` - run ESLint

## Next customizations

- Update `Your Name` in `src/layout/PortfolioLayout.tsx`
- Replace placeholder project data in `src/pages/ProjectsPage.tsx`
- Connect contact form submit handler in `src/pages/ContactPage.tsx`
